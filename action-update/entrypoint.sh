#!/bin/sh -l
python3 /main.py -k "${1}" -c "${2}" -e "${3}" -p "${4}" --env-tst "${5}" --env-stg "${6}" --env-prd "${7}" --aws-tst-tenant-id "${8}" --aws-tst-access-key-id "${9}" --aws-tst-secret-access-key "${10}" --aws-tst-region-name "${11}" --aws-stg-tenant-id "${12}" --aws-stg-access-key-id "${13}" --aws-stg-secret-access-key "${14}" --aws-stg-region-name "${15}" --aws-prd-tenant-id "${16}" --aws-prd-access-key-id "${17}" --aws-prd-secret-access-key "${18}"  --aws-prd-region-name "${19}" --revoke-access "${20}" --access-tickets "true";
